The given projects are includded in this project by defining the router for each of them.
to run the project :-
              -nmp install
              -npm install react--router-dom
              -npm start